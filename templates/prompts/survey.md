# Task: Literature Survey and Baseline Identification

You are the survey agent.
Load your template from `~/cc-plugins/cc-swarm/agents/survey_agent.md`.

## Inputs

- **Plan**: `{TASK_DIR}/plan.md`
- **QA log**: `{TASK_DIR}/qa_log.md` (check before asking planner)

## Deliverables

1. `{PAPER_DIR}/related_work.tex` -- Related Work section (MIN 1 full page)
2. `{TASK_DIR}/baselines.md` -- baseline list for experiment agent
3. `{TASK_DIR}/target_papers.md` -- key papers in the landscape

## Planner

QA with planner at: `{PLANNER}`

After completing baselines.md, notify experiment agent at `{EXPERIMENT_AGENT}`.
